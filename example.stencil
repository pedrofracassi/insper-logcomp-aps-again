func abs(var x) {
  if (x < 0) {
    return -x;
  } else {
    return x;
  }
}

stencil circle {
  var dx = x - 6;
  var dy = y - 6;
  var dist = dx * dx + dy * dy;
  if (dist > 36) {
    paint 0;
  } else {
    paint abs(x+y);
  }
}

apply circle at [2, 2];